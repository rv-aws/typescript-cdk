import { SubscriptionEmitter } from './emitter';
export declare class Receipt {
    protected emitter: SubscriptionEmitter<any, any, any>;
    protected event: string;
    protected hash: string;
    protected id: number;
    constructor(emitter: SubscriptionEmitter<any, any, any>, event: string, hash: string, id: number);
    /**
     * Closes the subscription.
     */
    close(): void;
    /**
     * Alias of the `close()` method.
     *
     * @see close()
     */
    unsubscribe(): void;
}
