"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class Receipt {
    constructor(emitter, event, hash, id) {
        this.emitter = emitter;
        this.event = event;
        this.hash = hash;
        this.id = id;
    }
    /**
     * Closes the subscription.
     */
    close() {
        this.emitter.remove(this.event, this.hash, this.id);
    }
    /**
     * Alias of the `close()` method.
     *
     * @see close()
     */
    unsubscribe() {
        return this.close();
    }
}
exports.Receipt = Receipt;
