import { Json } from '../serialize';
export declare class Request {
    /**
     * An identifier for this request. This won't be unique locally, but will always be unique for the sender.
     */
    readonly id: number;
    /**
     * The filter for this request.
     */
    readonly filter: any;
    /**
     * Constructs a new `Request` instance.
     *
     * @param id
     * @param filter
     */
    constructor(id: number, filter: Json);
    /**
     * Rejects the
     */
    reject(): void;
}
